.idea/
# Claude Code local skills / config (kept locally, not part of the shared repo)
.claude/
# IDA working files (never commit the ROM-bearing database)
*.i64
*.idb
*.id0
*.id1
*.id2
*.nam
*.til
# python
__pycache__/
*.pyc
